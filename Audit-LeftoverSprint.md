# Audit & Leftover Sprint Items

**Status:** Live tracking doc, replaces `SPRINT_PLAN_NEXTJS_REWRITE.md` going forward.
**Created:** 2026-07-24, from a pre-UAT audit of user- and supplier-facing pages (marketplace, financials/wallet, dashboard, passport; supplier analytics, inventory, requests, profile, financials, tutorials) plus every unchecked item still open in the old sprint plan.

`SPRINT_PLAN_NEXTJS_REWRITE.md` and `CLAUDE1.md` are now frozen historical records — useful for "why was it built this way," not for "what's left to do." This doc is the single source of truth for outstanding work from here on. New findings/gaps get added here, not back into the sprint plan.

---

## Part 1 — Audit Findings (2026-07-24)

Findings from a live click-through (real dev DB, `ethan@example.com` as member, `ben@acmecoworking.sg` as supplier) plus a static grep pass over every in-scope page/component. Scope was user-facing (Marketplace, Digital Passport, Financials/Wallet, Dashboard) and supplier-facing (Analytics, Inventory, Requests, Profile, Financials, Tutorials) pages — landing page, marketing sub-pages, and admin pages excluded.

### 🔴 High severity — fakes a completed action

- [x] **Marketplace "Submit Membership Inquiry" / "Request Consultation" fake-succeeds.** ~~`components/CustomRequirementsModal.tsx` — submitting fires **zero network requests** (confirmed via live network trace), yet shows *"Request Sent — our team will review your request..."*. A TODO in the code already acknowledges no backend exists (`handleSubmit` only does `setSubmitted(true)`). Fix: either wire a real endpoint, or change the copy to an honest "not available yet" rather than a fabricated confirmation.~~ **Fixed 2026-07-24**: real backend built — `MarketplaceEnquiry` model, `POST /api/marketplace-enquiries` (user submit), `GET`/`PATCH /api/admin/marketplace-enquiries` (admin queue + resolve). New "Pending Enquiries from Marketplace" row on the Admin Overview page opens a review modal (requester name/email/company, details, "Mark Fulfilled" button); enquiries are handled out-of-platform per product decision. Verified live end-to-end (submit → real DB row → admin queue → mark fulfilled → disappears from pending) plus 6 passing unit tests (`lib/marketplace-enquiries.test.ts`).
- [x] **Digital Passport "Edit Profile" doesn't save.** ~~`app/(user)/passport/page.tsx` — confirmed live: changed name, clicked Save, UI updated, reload reverted it. No `fetch`/mutation exists in the save path at all (`handleToggleEdit` only flips local state).~~ **Fixed 2026-07-24**: wired to a new `PATCH /api/me` (backed by `lib/user-profile.ts`), invalidates the `me` query on success. Verified live: name change survives a hard reload.
- [x] **Supplier Profile "Edit Profile" has the same bug, no disclosure.** ~~`app/(supplier)/supplier-profile/page.tsx` — identical dead toggle (`nameEdit`/`titleEdit`/`avatarEdit` never sent anywhere). Inconsistent with `BusinessDetailsCard` on the same page, which is wired correctly via `useUpdateSupplierCompany`.~~ **Fixed 2026-07-24**: same `PATCH /api/me` endpoint reused (shared `useUpdateProfile` hook); added the missing Save/Cancel pair (previously only a single toggle button existed). Verified live: name change survives a hard reload.

### 🟠 Medium severity — silently dropped data / stale copy

- [x] **Supplier decline-reason captured but never sent (bookings).** ~~`app/(supplier)/supplier-requests/page.tsx` / `lib/hooks/useSupplierBookings.ts` — `DeclineReasonModal` correctly captures a reason, but `useDeclineBooking`'s mutation sends no body. The backend route (`app/api/supplier/bookings/[id]/decline/route.ts`) already accepts `reason` — this is a pure frontend gap, quick fix.~~ **Fixed 2026-07-24**: `useDeclineBooking` now sends `{ reason }`, and `handleDeclineConfirm` passes the modal's reason through. Verified live end-to-end (declined a booking with a reason → confirmed `cancellation_reason` persisted in the DB).
- [x] **Same gap on bulk-order decline, but deeper.** ~~`handleDeclineBulkOrderConfirm` / `useDeclineBulkOrder` (`lib/hooks/useSupplierBulkOrders.ts`) — here the backend route (`app/api/supplier/bulk-order-requests/[id]/decline/route.ts`) doesn't accept a reason at all, so this needs a backend change too, not just a frontend fix.~~ **Fixed 2026-07-24**: added a new `BulkOrderRequest.declineReason` column (separate from the buyer-authored `cancellationReason` field used by the cancellation-request flow), migration applied, `declineBulkOrder` now persists it, the decline route accepts `reason` in the body, and the frontend hook/page wire it through. Verified live end-to-end (declined a bulk order with a reason → confirmed `decline_reason` persisted in the DB) plus a new passing unit test.
- [x] **Stale/misleading copy in three places** — reads like a leaked internal dev note instead of product copy:
  - ~~User Dashboard "Currently Active" card: *"Not wired yet — there's no GET endpoint to list active check-ins... Tracked as a backend gap."*~~ **Fixed 2026-07-24**: now reads "Coming soon — live check-in status will appear here once kiosk check-in is rolled out."
  - ~~Supplier Financials "Accounts Receivable, Receipts & Invoices" card: same pattern, verbatim engineering language.~~ **Fixed 2026-07-24**: now reads "Coming soon — invoice and receipt management will appear here in a future release."
  - ~~Wallet "Payment Methods" card: *"Stripe integration is planned for Sprint 6"* — false as written; Stripe is already live for booking charges (verified: Stripe Elements card iframe loads correctly with a real publishable key in the booking modal). The card really means "no saved-card management for wallet top-ups," but as worded it implies Stripe isn't integrated anywhere, which will confuse a UAT tester.~~ **Fixed 2026-07-24**: now reads "Saved-card management is coming soon. Top-ups are credits-only for now — for bookings, you can already pay by card at checkout."
  - Verified all three live in-browser (as both member and supplier sessions).
- [x] **`Listing.requireApproval` is dead — every booking requires supplier approval regardless of the flag.** ~~Found 2026-07-26 while fact-checking marketing copy on the new `/platform/list-and-fill` page. `Listing.requireApproval` (`prisma/schema.prisma`) is a real, supplier-settable column — surfaced in the listing create/edit APIs (`app/api/supplier/listings/route.ts`, `app/api/supplier/listings/[id]/route.ts`) and read back by `useListings`/`useSupplierListings` — but it is never once *read* anywhere in the booking-creation path. `createBookingWithDebit` (`lib/bookings.ts`) doesn't check it, and `Booking.status` unconditionally defaults to `pending` in the schema, so a booking always lands in the supplier's confirm/decline queue no matter what this flag is set to. A supplier who flips "no approval needed" off sees no behavior change at all. Needs `createBookingWithDebit` (or a wrapper) to read the listing's `requireApproval` and set the new booking straight to `confirmed` when it's `false` — likely still subject to the certificate-eligibility gate, which is a separate, correctly-wired check. Not started; the List & Fill page's "queue" section copy was rewritten to describe the *intended* per-listing-optional behavior, not the current always-on reality, on the understanding this gets fixed to match.~~ **Fixed 2026-07-27**: `createBookingWithDebit` (`lib/bookings.ts`) now takes an optional `requireApproval` param — the booking route (`app/api/bookings/route.ts`) passes through the fetched listing's own flag — and creates the booking straight into `confirmed` (instead of the schema default `pending`) when it's `false`, still gated behind the pre-existing certificate-eligibility check in the route (unchanged, runs before this function is even called). To keep the audit trail identical to a manually-confirmed booking, the auto-confirm path also writes the same zero-amount audit `Transaction` row, `booking_confirmed` `ActivityLog` entry, and `booking_confirmed` `Notification` that `confirmBookingWithAudit` writes on a normal confirm — verified this doesn't double-count in `getRevenueByCompany` (zero-amount row, same as the existing confirm flow). 4 new/updated tests in `lib/bookings.test.ts` (default-pending, auto-confirm + full audit-trail assertion, explicit `requireApproval: true` still pending); full `bookings.test.ts` (54), `revenue.test.ts`, and `supplier-payables.test.ts` suites all green; `tsc` clean.

### 🟡 Low severity / confirmed-accurate known gaps (not new, just re-verified)

- [x] ~~No `/notifications` page route exists (confirmed 404) — only the navbar dropdown panel. Not a bug, just still not built.~~ **Confirmed done (2026-07-28, re-verified)**: real full pages now exist at `/notifications` (user) and `/supplier-notifications` (supplier), both rendering `components/NotificationsPageContent.tsx` — backed by `useNotificationsPage`/`useMarkNotificationRead`/`useMarkAllNotificationsRead`/`useArchiveNotification` hooks against real `/api/notifications` routes (list w/ pagination + status filter, read-all, per-id read, per-id archive). `UserNavbar`/`SupplierNavbar` link to them (commit `c111f99`). Admin's `/admin-notifications` was already done. Stale item, was already built.
- [ ] "Currently Active" card has no backend (`GET` to list active check-ins doesn't exist, only `POST` create / `PATCH` check-out) — the copy issue above is separate from this; the underlying feature gap is real and intentionally deferred to kiosk/Sprint 5 work.
- [ ] All listing images render as broken/placeholder icons (Marketplace, Supplier Inventory, Profile) — expected, R2 isn't configured in this dev environment. Needs real R2 credentials before any real visual UAT pass, otherwise every listing looks broken to a tester.
- [x] **BuyerOrganization pooled "purchased credits" — spend write-path. DONE 2026-07-28.** ~~schema exists (`Transaction.buyerOrganizationId`), but no checkout UI lets a member choose personal vs. org funds.~~ Full pooled-spend system built per product-owner design: any member can top up the shared pool via a real Stripe charge (`POST /api/buyer-organization/topup`, same `createTopUp` path as personal top-ups, `TopUpOrgPoolModal.tsx`); the buyer org admin delegates two independent per-member permissions — `User.buyerOrgCanBook`/`buyerOrgCanPurchase` (Buy Now only, not bulk orders — those stay off-platform per F3) — via checkboxes in `ManageBuyerOrganizationModal`'s Members tab (admins implicitly have both). `FundingSourceSelector.tsx` (shared by `BookingModal`/`RequestPurchaseModal`) lets a member pick Personal vs. Organization at checkout: a permitted member funds directly from the pool (`createBookingWithDebit`/`createPurchaseWithDebit`'s new `buyerOrganizationId` param — no Stripe charge, pool debited atomically inside the DB transaction); a non-permitted member's pick instead creates a `BuyerOrgSpendRequest` (new model, `lib/buyer-org-spend-requests.ts`) — the admin is notified (`buyer_org_spend_request` notification type), reviews it in the Manage Organization modal's new "Spend Requests" tab, and either fulfills it (creates the real booking/purchase — mirroring the direct routes' own cert-gating/overlap/pricing checks — under the REQUESTING member's own account, never the admin's) or declines it with a reason. `getBuyerOrgPoolBalance`/personal-balance scoping added to `lib/credits.ts` (a fix mid-session: initially filtered by transaction type and silently missed booking-funded pool debits — corrected to sum all `buyerOrganizationId`-tagged rows, no type filter, mirroring `getCreditBalance`'s original "blind SUM" idiom). 16 new/extended tests (`lib/credits.test.ts`, `lib/buyer-org-spend-requests.test.ts`, plus additions to `bookings.test.ts`/`purchases.test.ts`/`buyer-organizations.test.ts`) — full suite (521 tests) green, `tsc` clean. Verified live end-to-end as real member/admin sessions: pool top-up (real Stripe charge), permission grant via UI, a direct org-funded booking, a non-permitted Buy Now → spend request → admin approval → real purchase, pool balance correct at every step. Follow-up idea from the product owner, not yet scoped: **build the same delegated-permission pattern on the supplier side too** (company admin delegating which employees can act on the company's behalf) — separate piece of work. ~~**DONE 2026-07-28 — see the entry directly below.**~~

- [x] **Supplier-side delegated permission for the "Boost Your Listings" catalogue (Bumps/Pin). DONE 2026-07-28.** The above buyer-org pattern used two hardcoded booleans (`buyerOrgCanBook`/`buyerOrgCanPurchase`) tied to the two specific buyer actions that exist today — the product owner's ask for the supplier side was explicitly broader: it shouldn't matter how many more products get added to the Bumps/Pin catalogue later (`Lab Digest`/`Ads`/`Newsletter` are already-stubbed "Coming soon" slots on the same card), they should all flow through one permission gate and one request/approve/decline pipeline, not a new boolean wired bespoke per product. Built accordingly: a single `User.companyCanPurchaseBoosts` flag (company admin grants/revokes it per member via a checkbox in `supplier-profile`'s Team Members card) gates the entire catalogue through one reusable auth helper (`requireCompanyBoostPurchasePermission`, `lib/supplier-auth.ts`), replacing the previous `requireCompanyAdmin`-only gate on the Bumps/Pin purchase routes. A member without the flag gets the same quantity/listing-picker UI in `ListingBoostCatalogueCard.tsx` but the action files a `CompanyBoostRequest` (new model, `lib/company-boost-requests.ts` — one generic `type: bump | pin` request row, mirrors `BuyerOrgSpendRequest`'s "nullable type-specific columns in one table" shape) instead of buying directly; the company admin is notified (`company_boost_request` notification type) and reviews it in a new "Pending Boost Requests" section (same card as "Pending Join Requests"), fulfilling it (calls the exact same `purchaseBumps`/`purchaseAndApplyPin` the direct routes already call, attributed to the requesting member's own userId, not the admin's) or declining it with a reason. Adding a future catalogue product (e.g. Ads) only needs a new enum value + a couple of nullable columns + one new fulfillment `case` — the permission gate and the whole request pipeline stay untouched. 12 new tests (`lib/company-boost-requests.test.ts`) — full suite (533 tests) green, `tsc` clean. Verified live end-to-end as real non-admin member/admin sessions (`chandra@acmecoworking.sg` / `ben@acmecoworking.sg`): non-permitted member's Bump request → admin approval attempt correctly blocked by insufficient company balance → company top-up → re-approval succeeds (bumpsAvailable incremented) → admin grants the permission checkbox → member now sees the direct Buy button instead of Request.

- [x] **Admin-manageable "Boost Your Listings" catalogue — replaces hardcoded Bumps/Pin pricing + static "Coming soon" placeholders. DONE 2026-07-29.** The entry above still left the catalogue itself hardcoded: `BUMP_UNIT_COST_CREDITS`/`PIN_DURATION_COST_CREDITS` were compile-time constants, and Lab Digest/Ads/Newsletter were static non-purchasable JSX with no way for admin to add a real product without a code change — flagged by the product owner while testing Supplier Inventory ("no way for a supplier to buy Bumps/Pin," not realizing the catalogue lived on Supplier Profile instead). New `BoostProduct` model backs the whole catalogue: `ListingBoostCatalogueCard.tsx` now renders exactly one card per active row — however many products exist is however many cards suppliers see, no hardcoded slots. Two kinds of rows: **builtin** (`builtinEffect: bump | pin`, seeded once by the migration) — admin can edit name/description/price and flip Active/Inactive but never delete, since `purchaseBumps`/`purchaseAndApplyPin` (`lib/company-credits.ts`/`lib/listings.ts`) now read price from these rows instead of the old constants and the routes assume exactly one of each exists; **custom** (`builtinEffect: none`) — freely addable/editable/deletable by admin at a new `/admin-boost-products` page (linked from the admin hamburger menu, `components/AdminBoostProducts.tsx`), and immediately purchasable by suppliers via a generic per-unit credit deduction (`purchaseBoostProduct`, `lib/boost-products.ts`) with **no manual admin fulfillment step** — confirmed explicitly with the product owner (who clarified "admin" meant SpaceSnap system admin, not the company-side delegation above, which is unchanged): the resulting `CompanyTransaction` row is itself the sale/revenue record, delivery of whatever the product represents happens outside the app. The existing delegated-request pipeline got the "new enum value + a fulfillment case" extension its own design comment anticipated (`CompanyBoostRequestType.product`, referencing a `BoostProduct`), so a non-permitted member's request for a custom product still routes to their company admin exactly like Bump/Pin. Supplier Profile also gained a promo header above the catalogue ("Use these to enhance your utilization rate!"), per product-owner ask — an earlier idea to duplicate the catalogue onto Supplier Inventory too was scoped back out in favor of just this one header. 16 new tests (`lib/boost-products.test.ts`) plus `lib/company-credits.test.ts` updated to assert against live seeded pricing instead of the removed constants — full suite (549 tests) green, `tsc` clean. Verified live end-to-end as real admin/company-admin sessions (`alice.admin@spacesnap.sg` / `ben@acmecoworking.sg`): admin catalogue page shows only Bumps/Pin with no delete option on either → added a custom "Lab Digest" product (100 credits) → appeared live on Supplier Profile alongside Bumps/Pin under the new header → direct purchase succeeded instantly with no approval step and showed up in the company's Financials Credit Movement ledger as `Purchased 1x Lab Digest -100 Credits` → deleted the product from the admin panel → disappeared from Supplier Profile immediately.
- [x] ~~Lab Digest / Ads / Newsletter cards on Supplier Profile — explicitly labeled "Coming soon," confirmed intentional, not a bug.~~ **Superseded 2026-07-29** — see the admin-manageable Boost Products catalogue entry above; these are no longer hardcoded placeholders, admin can add/edit/remove real, immediately-purchasable catalogue products directly.
- [ ] `lib/mockTutorials.ts`'s `VIDEO_CATEGORIES` populates two real rendered UI lists (filter pills + upload `<select>`) — technically "mock" file, but it's a legitimate fixed enum matching `TrainingVideo.category`, not fake content. Low-priority hygiene: move the constant out of a `mock*.ts`-named file so it stops looking like a flagged item.
- [ ] Marketplace map view uses a hardcoded `MAP_PIN_POSITIONS` array for pin layout (real listing data, fake/looping positions) — honestly labeled on-screen as "Sample map preview." Cosmetic only.

### ✅ Confirmed fixed (sprint-plan doc was stale — do not re-flag these)

- Sign-out works correctly on both navbars (`UserNavbar.tsx`/`SupplierNavbar.tsx` both call real `signOut()`) — session confirmed fully cleared live. The old sprint plan's Sprint 1 note calling this unwired is stale.
- Booking/purchase/bulk-order/cancel/modify modals (`BookingModal`, `RequestPurchaseModal`, `CancelBookingModal`, `ModifyBookingModal`, `RequestCancellationModal`, `ConfirmBulkOrderModal`, `CancellationReviewModal`) all call real mutations.
- `BuyerOrganizationCard`/`ManageBuyerOrganizationModal`/`OrgSearchInput` — fully wired; closes the original Sprint 7.1 trigger bug (signup's dead `company`/`role` fields).
- `RoleGuard.tsx` — member/supplier exclusivity genuinely enforced client-side, matching the server-side `proxy.ts` gate.
- Analytics, Inventory, Requests, Tutorials, Financials (supplier) pages all load cleanly with real data, no console errors, no failed API calls, across both a member and a supplier session.

---

## Part 2 — Leftover Items from the Old Sprint Plan

Pulled from every remaining unchecked `[ ]` box in `SPRINT_PLAN_NEXTJS_REWRITE.md`, deduplicated and reorganized by risk/theme (not sprint number). Where a parent line was unchecked but its sub-items had already closed, only the genuinely-still-open piece is listed.

### Payments & Compliance (highest risk — financial/legal, needs a second reviewer)

- [ ] ⚠️ Developer review required before going live on Stripe — financial/compliance risk (Sprint 6).
- [ ] No live payment code merged without a second reviewer (Sprint 6 checklist).
- [ ] One real booking's Stripe charge + webhook round-trip verified against a **deployed** URL, not just `stripe listen` on localhost (Sprint 6.11). Note: the Stripe Elements card field loads correctly and charge-on-create/webhooks/refunds are code-complete per the old plan — this is the one remaining "run it for real once" step before sign-off.
- [ ] "Credits" cosmetic-unit ToS clarification section still needs to be written into an actual Terms of Service document/page (flagged in old Sprint 7, never drafted) — the exact wording was already drafted in the sprint plan and just needs a home.
- [x] ~~**No commission breakdown ledger — SpaceSnap's own cut is computed and discarded, never stored.**~~ **Cleared — see Part 3 F2 below (Parts A–D + F5, "F2 is now complete").** `SupplierPayable.commissionAmount` is now a real persisted column, and the admin Financials "Supplier Payouts" card surfaces the full three-way reconciliation (gross collected → commission kept → supplier net) both platform-wide and per payout batch. Real Xero API sync is still future/manual, unchanged.
- [ ] **Full audit of money movement needed, end to end — this is the product owner's main revenue and has to produce auditable statements.** Raised 2026-07-24 directly in response to the commission-ledger gap above. Scope: trace every dollar through the whole pipeline — member's Stripe charge → platform commission taken → `SupplierPayable`/`SupplierPayout` batch → manual Xero Bill/Remittance today (real Xero API sync later) — and confirm at each hop that the money is fully accounted for and reconcilable (nothing created or destroyed between stages, every credit-denominated figure traceable back to its true-SGD source per the `lib/credit-units.ts` convention). Distinct from the commission-ledger item above: that one is "store the commission figure"; this one is "verify the whole chain reconciles and produce statements an auditor could check," which is a bigger review, likely wants a second reviewer given the "Payments & Compliance" risk tier this section is already flagged at. Not started — no design decisions made yet on what the audit method or the statement format should look like.

### Deployment Readiness (blocks a real go-live, Sprint 6.11)

- [x] A real deploy exists at a Railway URL, logged into successfully. **Done (per product owner, 2026-07-27)** — not independently re-verified in this session.
- [ ] CORS/cookie behavior confirmed in a deployed (not just localhost) environment.
- [ ] R2 bucket CORS configuration for the production bucket.
- [x] Separate R2 bucket + credentials for production (distinct from the dev `spacesnap-dev` bucket). **Done (per product owner, 2026-07-27)** — not independently re-verified in this session.
- [ ] Stripe webhook endpoint pointed at the real Railway URL, with a real (non-`stripe listen`) signing secret set as `STRIPE_WEBHOOK_SECRET`. **Deliberately still sandbox** — per product owner 2026-07-27, staying on Stripe test/sandbox until the application is deemed fully clear; do not switch to live keys/webhook without explicit go-ahead.
- [ ] Railway Cron Schedule service provisioned for `/api/cron/resolve-pending-booking-credits` (currently only a lazy read-time sweep covers this).
- [ ] Railway environment variables set from real production values (`DATABASE_URL`, fresh `AUTH_SECRET`, `R2_*`, Stripe keys, `STRIPE_WEBHOOK_SECRET`, `CRON_SECRET`) — not copied from local `.env`.
- [ ] Confirm Railway's managed Postgres connection-pool/`sslmode` behavior under Prisma, especially across container restarts/redeploys — not yet verified against a real Railway Postgres instance.
- [ ] `output: 'standalone'` in `next.config` — optional (smaller/faster build), not a blocker.

### Kiosk / Middleware (Sprint 5 — separate hardware track, not started)

- [ ] Separate API surface for kiosk hardware auth.
- [ ] Coordinate scope with the funded POC middleware work (Trust Architecture: Pi decides locally, this API only ever supplies credential facts, never an authorization verdict).
- [ ] Confirm with middleware spec (v1.3) that no endpoint here could be mistaken for/misused as an authorization decision endpoint.
- [ ] Check-ins UI is explicitly **not** to be built browser-side per the kiosk Trust Architecture — a `CheckIn` must only ever be written by the physical kiosk's Pi after a local credential match + card dispense. This is the root cause of the Dashboard's "Currently Active" gap above — don't build a browser workaround for it.

### Deferred-by-design feature gaps (product decisions, not bugs)

- [ ] **Gigs** — schema landed (`GigTask`/`GigAssignment`), no write path, no UI. Explicitly shelved twice by the product owner. Do not silently start building without re-confirming it's back in scope.
- [x] ~~**Product-facing "tier" concept for `earning_method`** — the gating mechanism (self-serve video/quiz vs. operator sign-off vs. operator-or-SME sign-off) is built and tested, but whether/how this is ever surfaced to users as a labeled concept is undefined.~~ **Done 2026-07-28**: surfaced as a "See how [certificates are earned / it works]!" pill next to the subheader on both the member Digital Passport (`app/(user)/passport/page.tsx`) and supplier Training Management (`app/(supplier)/supplier-tutorials/page.tsx`) pages. Each opens a modal (`components/HowCertificatesAreEarnedModal.tsx` / `components/HowSupplierTrainingWorksModal.tsx`) showing a tier-explainer infographic hosted on the R2 public bucket (`marketing/passport/user-credentials-earned.png`, `marketing/passport/supplier-list-credentials.png`). Verified live in both member and supplier sessions; `tsc` clean.
- [ ] **`events` (Exclusive Event Invite) and `lucky_draw` (Lucky Draw Ticket)** reward-catalogue categories — explicitly deferred, product owner still deciding the events side of the business. Shape for `lucky_draw` is pre-agreed (draw on a set date) for whenever it's picked back up.
- [x] **Personal/Others spend-attribution toggle for the BuyerOrganization pooled credit pool. DONE 2026-07-30.** ~~has no home yet since there's no pooled-spend write path to attribute (see the pooled-credits gap in Part 1).~~ That pooled-spend write path landed 2026-07-28 (see above), so the feed to attribute now exists. The only mixed-user feed for the shared pool is the org-admin-only "Credit Movement" list in `ManageBuyerOrganizationModal`'s Overview tab (`components/ManageBuyerOrganizationModal.tsx`) — the personal wallet page (`/wallet`) is single-user, so there was nothing to toggle there. Added a 3-way `All / Personal / Others` pill filter next to the existing date-range picker: `getBuyerOrgTransactions`/`serializeBuyerOrgTransaction` (`lib/buyer-organizations.ts`) take a `scope: "all" | "personal" | "others"` param, filtering server-side (`user.id = viewer` / `user.id != viewer`) so pagination totals stay correct; `GET /api/buyer-organization/transactions` gained a validated `?scope=` query param, defaulting to `all`. 2 new/extended tests in `lib/buyer-organizations.test.ts` (scope filtering + existing pagination test updated for the new signature) — full suite green, `tsc` clean. Verified live end-to-end against a temporary fixture org/admin/member (created and torn down via a throwaway script, not left in the dev DB): All shows all 3 rows, Personal shows only the admin's own top-up, Others shows only the member's 2 spend rows.

### Sprint 7 — Final Polish & Re-Verification (never completed)

- [ ] Supplier dashboard: manage spaces, view bookings, access logs — largely covered today by Inventory/Requests/Analytics; worth a final check that nothing specific is still missing rather than treating this as a fresh build.
- [ ] Final responsive/polish pass across the app.
- [ ] Re-run the full PreUAT checklist against the new stack — every item that passed on the old Bubble/Laravel build re-verified here, not assumed carried over.
- [ ] Financials/audit-trail spot check — confirm revenue-by-operator figures are complete now that the Sprint 3.5 transaction gaps are closed.
- [ ] Side-by-side smoke test against the old Laravel/Vite build for any page where behavior differs.
- [ ] Old Laravel/Vite build kept live and untouched as a fallback until the new stack has run cleanly for a defined period (owner's call on how long).

### Marketing Sub-Pages (Sprint 7.12/7.13 — out of this audit's scope, listed for completeness)

- [x] ~~`/solutions/startups`, `/solutions/space-providers`, `/solutions/suppliers` — all currently just a "coming soon" `MarketingPageShell` stub, content not written.~~ **Superseded 2026-07-26 — the whole `Solutions` section was removed; see the dedicated note below the List & Fill entry for what happened to each of these three.** **`/platform/marketplace` DONE 2026-07-25**: built out the "Features" marketing page from the imported Claude Design project (`components/marketing/MarketplaceFeatures.tsx`, wired in `app/platform/marketplace/page.tsx`), reusing the shared `MarketingNavbar`/`MarketingFooter`. Kept the design's aggressive scale + scroll-reveal/marquee motion but **re-skinned onto SpaceSnap's own system** — teal accent (`#4db8b0`/`#1a9d96`), site background (`#0a0e14`), Geist sans/mono — so it harmonises with the landing page (the source's neon-green + Archivo look was dropped). Also: added **Platform** + **Solutions** dropdowns to `MarketingNavbar` (between About Us and Partners); added a reusable reduced-motion-aware `components/marketing/Reveal.tsx` and applied a light scroll-reveal to the landing page (`app/page.tsx`) benefit cards + sections. Copy: source's "No lease"/"long lease" reworded per the no-"lease" constraint; card 3 now frames the **market's** 3-month-bench / 6-month-suite minimums as the status quo SpaceSnap beats with day/week/month booking. The marketplace page's two "Digital Passport" CTAs now resolve — see below.
- [x] **`/platform/digital-passport` DONE 2026-07-25**: built from the same Claude Design project's `Digital Passport.dc.html` (imported via the design MCP). New `components/marketing/DigitalPassport.tsx`, wired in `app/platform/digital-passport/page.tsx` with the shared `MarketingNavbar`/`MarketingFooter`, replacing the `MarketingPageShell` stub. Same porting approach as the marketplace page: kept the design's scale + `data-reveal` scroll-reveal (reduced-motion aware) but **re-skinned onto SpaceSnap's system** — teal accent (`#4db8b0`/`#7fd4cd`/`#1a9d96`), `#0a0e14` bg (`#0b1017` for the "difference" band), Geist sans/mono — dropping the source's `#34E3A8` neon-green + Archivo/IBM Plex look and its own header/footer. Sections: hero ("Prove it once. Work anywhere." + 3 stats + a portrait 4:5 real passport-UI screenshot), "What's inside" (4 issuer-attributed credential rows), "The difference it makes" (Traditional-onboarding vs Digital-Passport before/after, Weeks → Same day), and a teal-gradient sign-up CTA band → `/signup`. Scoped `.dp-*` CSS (fluid type + responsive/hover) added to `app/globals.css`, reusing the existing `ss-pulse-glow` keyframe. `tsc` clean; verified in-browser (no console errors, correct Geist/teal computed styles, full DOM structure).

  **Hero image (2026-07-25):** the hero slot now shows a real example passport UI, not a placeholder — proves the R2 public-bucket pipeline end-to-end (contradicts the old Part 1 audit note that R2 isn't configured in this dev env; the **public** bucket, `R2_PUBLIC_*`/`R2_PUBLIC_BASE_URL`, is fully configured and live — only the *private* evidence bucket is untested from a UI, and R2 CORS/prod-bucket items in Part 2's Deployment Readiness section are unaffected). Since screenshotting the real logged-in `/passport` page needs a session (headless auth-cookie minting was correctly blocked by the safety classifier as looking like session forgery), built `public/passport-preview.html` instead — a standalone, no-auth example passport card (fake user "Alex Tan", SpaceSnap-styled) sized to a clean 4:5 frame, meant to be opened and screenshotted by a human. User captured it, saved to `public/`, confirmed via `Read` it was the right image; uploaded to the R2 **public** bucket at `marketing/digital-passport/passport-ui.webp` (verified with a real `PutObjectCommand` → `HeadObjectCommand` → public `GET` returning `200 image/webp`), then wired into `DigitalPassport.tsx` as a plain `<img>` (not `next/image` — no `remotePatterns` entry exists for the R2 domain, and this component doesn't use `next/image` elsewhere) pointed at the R2 public URL. Verified live: image loads from the R2 URL (not a local fallback — the local copy was deleted before the final check), correct `naturalWidth`/`complete`, no console errors. `public/passport-preview.html` is kept in the repo as a reusable capture tool for updating this image later.

  **Hero background consistency (2026-07-25):** the hero's background grid (ported from the Claude Design source, later "fixed" to match `MarketplaceFeatures.tsx`'s grid) was flagged as inconsistent with the site's actual precedent — the landing page (`app/page.tsx`) hero uses ambient radial glows only, no grid overlay. Removed the grid `backgroundImage`/`backgroundSize` div from both `DigitalPassport.tsx` and `MarketplaceFeatures.tsx` (kept their glow blobs, which do match the landing page's look). Both marketing pages now render a plain glow-only hero background, consistent with `app/page.tsx`. Verified live on both `/platform/digital-passport` and `/platform/marketplace`; `tsc` clean, no console errors.
- [x] **`/platform/list-and-fill` DONE 2026-07-26**: built from the same Claude Design project's `List and Fill.dc.html` (imported via the design MCP), filling one of the two former "Supplier Feature (Undecided)" `Platform` nav slots. New `components/marketing/ListAndFill.tsx`, wired in `app/platform/list-and-fill/page.tsx` with the shared `MarketingNavbar`/`MarketingFooter`; `MarketingNavbar` gained an `accent="teal" | "purple"` prop so its Sign In button matches the page's theme (default teal for member-facing pages, purple here). Unlike the member-facing Digital Passport/Marketplace pages, this one targets operators/suppliers, so it's re-skinned onto the site's own **`supplier-purple`** tokens (`#9333ea`/`#6b21a8`/`#c084fc` from `tailwind.config.ts`) rather than teal — conveniently the design's own native violet palette already matched those exact values, so this was closer to a direct port than a reskin. Sections: hero (floorplan-zone visual, live toggle-driven "idle/earning" legend), "List in minutes" (3 listing-type cards — space/equipment/consumables — each with a real interactive availability toggle), "The queue runs itself" (incoming-request queue with working Confirm/Decline buttons and a live "N awaiting you" count, cross-linking to `/platform/digital-passport`), "Revenue, visible" (net-payout-by-listing-type stacked bar chart, sample data labeled as such), "Fair, either way" (decline-penalty tiers), and a closing sign-up CTA → `/signup`. Also added to `MarketingFooter`'s Platform links. Kept the source design's toggle/confirm-decline interactivity genuinely working (local `useState`, no backend) rather than flattening to a static snapshot, since it's cheap and demonstrates real product behavior. Scoped `.lf-*` CSS (fluid type + responsive) added to `app/globals.css`, reusing the existing `ss-pulse-glow` keyframe. `tsc` clean; verified via DOM/console inspection (in-browser screenshot capture was unreliable this session — a Browser-pane rendering glitch, not a page issue — so correctness was confirmed via computed styles, text content, and simulated clicks instead).

  **Fact-check correction pass (2026-07-26):** the source design invented product behavior that doesn't exist, confirmed against `lib/booking-policy.ts`, `lib/supplier-payables.ts`/`lib/supplier-payouts.ts`, `lib/revenue.ts`, and this doc's own F2/F3/F5 findings:
  - **"Get paid on your terms" removed entirely.** The design's Free/Preferred/Top payout-cadence tiers (monthly/bi-weekly/weekly, supplier-selectable) never existed as built — `lib/booking-payments.ts` confirms cadence was flattened to biweekly for every tier on 2026-07-23 (tier only affects the decline-rebate %, not frequency) and payouts are a fully manual, admin-batched `SupplierPayout` process (admin picks the period, pastes a Xero Bill URL then a Remittance Advice URL) — never a supplier-facing choice.
  - **"Revenue, visible" rewritten.** The design's "via SpaceSnap vs. your own company" split has no backing anywhere in the schema — the real "My Earnings" chart (`getCompanyNetPayoutByTypeAndMonth`) only ever sums `SupplierPayable.netAmount` (money actually paid out, net of commission) bucketed by **listing type** (space/equipment/consumables). Rebuilt the chart around that — 3-way stacked bars by type instead of the fictional 2-way split — and added an honest one-line footnote ("paid out every two weeks") in place of the deleted tier section, rather than leaving payout cadence unmentioned entirely.
  - **"Protected either way" → "Fair, either way", numbers reversed.** The design claimed a user's cancellation pays the supplier compensation on a sliding scale ("the more you're covered"). `lib/bookings.ts`'s `cancelBookingWithRefund` proves the opposite: a user-cancelled booking is a **zero-effect** `SupplierPayable` row regardless of notice — the supplier is unaffected, never compensated. The real sliding scale (`calculateSupplierCancellationPenalty`, 0%/50%/100% by 7-/3–6-/<3-day notice) instead penalizes the **supplier's own** late decline, forfeiting that much of SpaceSnap's commission share. Rewrote the section to state both facts correctly: member cancellations cost the supplier nothing, and the tiered cards now correctly show what a supplier forfeits by declining late — the opposite framing from the original design.
  - **Bulk-order queue row fixed.** Was showing "1,520 cr" as if a real charge, which contradicts F3 (bulk orders settle **off-platform**, no credits move). Now shown as "~1,520 (indicative)" in muted styling, matching the same "indicative only" precedent already applied to `RequestPurchaseModal`.
  - **Regional terminology**: all "Bay" labels (floorplan zones, the LC-MS location) changed to "Suite" per product-owner correction (not used in this region); "Cold" → "Cold Storage", "Loading" → "Tissue Culture Room" for clarity.
- [x] ~~1 more `Platform` nav entry — labeled "Supplier Feature (Undecided)", still needs a name before its route can exist.~~ **Resolved 2026-07-26**: the slot became "Why SpaceSnap?" — see below.
- [x] ~~1 more `Solutions` nav entry — "For Larger Companies" (undecided), wants to market the regional-mobility angle.~~ **Moot 2026-07-26**: product owner dropped the entire `Solutions` nav section — see below.
- [x] **`Solutions` nav section removed; `/solutions/startups` renamed + moved into `Platform` as "Why SpaceSnap?" DONE 2026-07-26**: product owner decided there's no need for a standalone `Solutions` dropdown at all. `MarketingNavbar`/`MarketingFooter` no longer have a Solutions entry. Per-page disposition of the three former `/solutions/*` routes:
  - `/solutions/space-providers`, `/solutions/suppliers` — always just `MarketingPageShell` "coming soon" stubs (never had real content) — **deleted outright**, no nav slot left to point them at.
  - `/solutions/startups` (`components/marketing/ForStartups.tsx`, the one page with real built-out content) — **kept and moved**, not deleted: now `/platform/why-spacesnap` (component renamed `WhySpaceSnap.tsx`), sits first in the `Platform` dropdown and footer column, nav label "Why SpaceSnap?". This also fills the old "Platform nav entry — Supplier Feature (Undecided)" slot noted above. Body copy/H1 (still startup-audience-specific — "Big-lab capability. Startup-sized commitment.") deliberately left as-is, matching the "only asked for nav placement, not a content rewrite" scope; only the eyebrow badge changed, through two rounds — first "Solutions · For Startups" → "Platform · Why SpaceSnap?" (styled as a proper pill, matching every other marketing page's hero badge — the original port had left it as unstyled flex text), then reworded again to "SpaceSnap solves your problems" per direct product-owner instruction. The H1's accent span ("Startup-sized commitment.") also picked up the same `textShadow` glow treatment already standard on the other marketing pages' hero accent text (e.g. List & Fill's "Most of it is asleep.").
  - The `Solutions` footer column isn't gone, just repurposed: product owner's explicit call was to keep the 4-column footer shape and put `Resources` there instead (Product Roadmap / SpaceSnap Insights / Latest Releases — previously only in the navbar dropdown, never in the footer). Footer is now Platform / Resources / Get in touch, plus the brand column.
- [ ] **Only remaining marketing-subpage gap: the 3 `Resources` routes have no pages.** `/resources/product-roadmap`, `/resources/insights`, `/resources/latest-releases` are linked from both the navbar's `Resources` dropdown and (as of 2026-07-26) the footer, but no `app/resources/*` route exists — confirmed 404. Everything else under `Platform` is now built (Marketplace, Digital Passport, List & Fill, Why SpaceSnap?); `Solutions` no longer exists as a section, so this is the only marketing sub-page content gap left.
- [ ] Scroll/hover/page-load transition effects across marketing pages — scope not yet defined, needs a follow-up conversation before implementation.

---

## Part 3 — Full Money-Movement Audit (2026-07-24)

The end-to-end trace requested in Part 2 ("Full audit of money movement needed, end to end"). Static trace of every dollar through the pipeline, hop by hop, confirming reconciliation at each stage. **Headline: the write paths reconcile correctly, but the *reporting* layer (`lib/revenue.ts`) was never rewired to the split-ledger `TransactionType` values, so every revenue figure shown to admins and suppliers is wrong.**

### The pipeline, hop by hop (what actually gets written)

1. **Member pays (money in).**
   - Booking (space/equipment): full-price Stripe PaymentIntent → `Transaction` type `booking_payment` (negative). Charged *before* the DB txn opens, with a compensating Stripe refund in the catch block — atomic. ✅
   - Reschedule: Stripe charge → `booking_modification_fee` (negative). ✅
   - "Buy Now" consumables: debited from `purchasedBalance` → `purchased_spend` (negative), no Stripe (wallet-funded). ✅
   - Bulk-order fulfillment: **off-platform as of 2026-07-25 — no credits move at all** (supplier settles RSP directly, SpaceSnap takes no cut). The old on-platform debit → `purchase` is shelved behind `BULK_ORDER_ON_PLATFORM_SETTLEMENT` (default off). ✅ see F3 (resolved).
   - Wallet top-up: `purchased_topup` (positive), now backed by a real Stripe card charge (F4 resolved 2026-07-25). ✅
2. **Commission split.** `createCompletedBookingPayable` computes `commissionAmount` then discards it, storing only `grossAmount`/`netAmount`. ⚠️ see F2.
3. **Supplier payable ledger.** `SupplierPayable` rows (completion credit / decline-penalty debit / cancel zero-row), balance = live `SUM(netAmount WHERE pending)`. Coherent, nets penalties against earnings automatically. ✅
4. **Payout batch.** `SupplierPayout` snapshots the summed `netAmount`, manual Xero Bill/Remittance. Coherent. ✅
5. **Refunds.** cancel / decline-resolution / booking-credit → `refund` (positive) + real Stripe refund; `stripe-webhooks.ts` cross-checks Stripe's `amount_refunded` against the ledger. ✅
6. **Revenue reporting.** ❌ **BROKEN — see F1.**

### Findings

- [x] 🔴 **F1 (CRITICAL, confirmed) — `lib/revenue.ts` sums stale transaction types; the platform's primary revenue is invisible and refunds make revenue go negative.** ~~`REVENUE_TRANSACTION_TYPES = ["booking", "purchase", "refund"]`, but the split-ledger rewrite moved real charges to `booking_payment` (all space/equipment bookings — the main revenue stream), `purchased_spend` (all Buy Now sales), and `booking_modification_fee` (reschedule fees) — **none of which are summed**. The only sale type still counted is bulk-order `purchase`. Worse, `refund` (positive) *is* still summed and then negated, so a completed booking reports **0 revenue** and a refunded booking reports **negative** revenue. This directly breaks `revenue.ts`'s own stated invariant ("a booking that was declined contributes 0, not -amount") — that only held when the debit was type `booking`, which now only ever carries a zero-amount confirm-audit row. Affects every money figure the app shows: Admin Overview "total revenue" card, Admin Financials revenue-by-operator table + cross-company feed, and the Supplier Financials "Platform Revenue" chart. **No test coverage** (`lib/revenue.test.ts` does not exist).~~ **Fixed 2026-07-24**: added `booking_payment`, `booking_modification_fee`, and `purchased_spend` to `REVENUE_TRANSACTION_TYPES` (kept the legacy `booking`/`purchase`/`refund` types so seed/pre-transition rows and refund-netting still work; no double-counting — a live booking writes one `booking_payment` charge plus a zero-amount `booking` audit row, and earned-credit discounts stay excluded). Note on why the earlier Stripe testing masked this: `prisma/seed.ts` writes old-type `booking` rows with real amounts, so the Financials pages always showed *seed* revenue (~4200 credits) — a real Stripe charge writes `booking_payment`, which the query ignored, so the number validated came from the seed, not from the charge. Added `lib/revenue.test.ts` (6 passing DB-integration tests against real Postgres): live `booking_payment` counts, Buy Now `purchased_spend` counts, a fully-refunded booking nets to 0 (not negative), legacy `booking`/modification-fee rows still count, `earned_spend`/`earned_grant` stay excluded, and the per-type monthly chart buckets `booking_payment`→space / `purchased_spend`→consumable. Registered in the `test` script. Not browser-verified: the dev DB currently holds only seed data (zero live `booking_payment`/`purchased_spend` rows), so the Financials pages render identically pre/post-fix — the isolated DB test is the demonstrating proof. This closes the old Sprint 7 "confirm revenue-by-operator figures are complete" item.
- [x] 🟠 **F2 (High, = Part 2 commission-ledger gap) — commission computed and discarded, AND the whole pricing/commission model was wrong.** Confirmed originally: `SupplierPayable` had no `commissionAmount` column. **In the course of fixing this (2026-07-24) the product owner clarified the real model, which the code did not implement at all:**
  - Suppliers list a **base** price. The **marketplace price a member pays is base × a per-booking-type markup** — daily +50%, weekly +30%, monthly +20%. The code applied **no markup whatsoever** (`cost = listing.priceDay` charged directly) — SpaceSnap was collecting only its commission and losing its entire margin.
  - **Booking commission**: SpaceSnap keeps the markup **plus 10% of base**; the supplier is paid **90% of base** regardless of booking type. (Worked example: base 100 → member 150, supplier 90, SpaceSnap 60.) The code used a flat 10% of the charged price.
  - **Consumables**: member pays RSP (no markup), SpaceSnap keeps **7%**, supplier 93%. The code created **no supplier payable for consumables at all** (`SupplierPayable.bookingId` is required — structurally booking-only), so consumables had no payout ledger or commission.
  - **All rates must be admin-controlled, per-supplier**, defaulting to the platform values (50/30/20 markup, 10% booking, 7% consumables) when a supplier isn't given custom rates. Suppliers get no pricing UI — admin sets everything.

  **Part A — DONE & tested (2026-07-24):** admin-controlled, per-supplier pricing config. New `PlatformPricingConfig` singleton (admin-editable defaults) + nullable per-company override columns on `Company` (`booking_markup_{daily,weekly,monthly}_percent`, `booking_commission_percent`, `consumables_commission_percent`), migration `20260724150000_platform_pricing_config` (seeds the defaults row, backfills `Booking.base_amount = sgd_amount` for pre-markup rows). `lib/pricing.ts` resolves effective rates (override ?? default) and holds the markup/commission math. New `GET/PATCH /api/admin/pricing` + `PATCH /api/admin/pricing/companies/[id]`, and a **"Pricing & Commission" admin panel** (`components/PricingCommissionCard.tsx`) on the admin Financials page (placement easy to change) — platform defaults editor + per-supplier override table (blank = inherit). `commission_amount` column added to `SupplierPayable` (migration `20260724140000_supplier_payable_commission`, backfilled) and `base_amount` to `Booking`. 7 passing tests in `lib/pricing.test.ts` (incl. the 150/90/60 worked example + override resolution). `tsc` clean; existing `supplier-payables.test.ts` still green. **Charges are unchanged so far** — Part A only adds the control surface; the app stays in a consistent no-markup state until Part B wires it in.

  **Part B — DONE & tested (2026-07-24):** markup wired into the actual charge + payout, display in lockstep. The booking route (`app/api/bookings/route.ts`) now charges `base × effective markup` (`getEffectiveCompanyPricing` + `applyMarkup`) and passes the base + effective commission through `createBookingWithDebit` (stored as `Booking.baseAmount` / `platformCommissionPercent`). `createCompletedBookingPayable` pays the supplier `supplierGrossForBase` (= base × (100−commission)%) and stores `commissionAmount = sgdAmount − gross` (the markup + commission of base); `declineBookingPendingResolution`'s penalty is now sized against that real commission. Display stays in lockstep: `serializeListing` takes an optional pricing context — **user-facing** routes (`/api/listings`, `/api/listings/[id]`) serialize the **marked-up** price (batched `getEffectiveCompanyPricingMap`), **supplier-facing** routes keep serializing the **base** — so the marketplace/BookingModal show exactly what the route charges with no frontend changes. Verified: base 120 → marketplace 1800 credits = charged, supplier view 1200 (base). New end-to-end test in `supplier-payables.test.ts` (base 100 → supplier 90 / SpaceSnap 60); `bookings.test.ts` (51) + `pricing.test.ts` (7) + `supplier-payables.test.ts` (5) all green; `tsc` clean. *(Env note: `prisma generate` under the running `next dev` wedged the dev server — needs a manual `npm run dev` restart; unrelated to the code.)*

  **Part C — DONE & tested (2026-07-24):** consumables 7% payout ledger. Generalized `SupplierPayable` beyond bookings — `bookingId` is now nullable and two new nullable `@unique` sources (`purchaseId`, `bulkOrderRequestId`) were added, with a `supplier_payables_exactly_one_source` CHECK constraint (migration `20260724160000_supplier_payable_consumables`). New `createConsumablePayable` (`lib/supplier-payables.ts`) pays the supplier `supplierGrossForConsumable` (= RSP × (100 − effective consumables commission)%) and stores `commissionAmount = charged − gross`; wired into `createPurchaseWithDebit` (Buy Now) and `fulfillBulkOrderWithDebit` (both terminal, so no reversal path). **Superseded for bulk orders 2026-07-25 (see F3):** the `bulkOrderRequestId` source and the bulk-order `createConsumablePayable` call now only fire under the shelved `BULK_ORDER_ON_PLATFORM_SETTLEMENT` flag — off-platform (default), bulk orders take no cut, so only the Buy Now (`purchaseId`) 7% payout is live. The generalized schema/CHECK stay in place for both the shelved path and Buy Now. Consumable payouts batch + reconcile through the exact same `SupplierPayout` flow as bookings, no changes needed there. Tests: Buy Now end-to-end (RSP 100 → supplier 93 / SpaceSnap 7, flows into pending balance) + the exactly-one-source CHECK; fixed a pre-existing `check-ins.test.ts` fixture that built bookings without `baseAmount`. `supplier-payables`/`purchases`/`bulk-orders` (41) + `check-ins` (11) + `reward-tiers` (23) green; `tsc` clean.

  **Part D — DONE & tested (2026-07-25):** three-way reconciliation surfaced in the admin panel. `getPayableReconciliation(where)` (`lib/supplier-payables.ts`) derives `grossCollected = commissionKept + supplierNet` (commissionKept = `commissionAmount` + `penaltyDeduction`) entirely from stored columns, plus `getPlatformReconciliation` (all-time) and `getReconciliationByPayoutIds` (per batch). Wired into `GET /api/admin/supplier-payouts` (platform headline + per-pending-company + per-batch) and rendered on the admin Financials "Supplier Payouts" card (a platform reconciliation strip + an inline "members paid = SpaceSnap + supplier" line on every Ready-to-Bill and Awaiting-Payment row) so an admin can verify the split before the manual Xero Bill step. Covers consumables automatically (they store `commissionAmount`). Test: worked example (2 completed + 1 declined → gross 10 = commission 1.5 + supplier 8.5, identity asserted); verified live against the dev DB (identity holds). `tsc` clean.

  **F5 — RESOLVED (2026-07-25):** the supplier's own chart now shows **net payout**, not the marked-up member price (product owner: "that's what matters to them"). `getCompanyRevenueByTypeAndMonth` → renamed `getCompanyNetPayoutByTypeAndMonth`, now sums `SupplierPayable.netAmount` by listing type/month (90% of base per booking, 93% of RSP per consumable, minus decline penalties) instead of `booking_payment` transactions; supplier-dashboard card relabeled "My Earnings — your net payout … after SpaceSnap's commission." Admin-facing revenue (`getRevenueByCompany`, platform summary) still shows gross marketplace volume, unchanged. Test updated to the net-payout semantics.

  **Margin privacy (2026-07-25):** suppliers must never see the marketplace markup — they key in their base and know only their commission rate. The supplier bookings/requests views were printing `booking.sgdAmount` (the marked-up member price), from which the margin was derivable. `serializeBooking` is now audience-aware (`forSupplier`): supplier routes return `supplierNet` (base × (1 − commission%), shown as "You earn N credits") and omit the member-paid price + member discount; member routes are unchanged and never receive `supplierNet` (so the base can't be derived from the member side either). Consumables (bulk-order `credits` = the supplier's own RSP, no markup) don't leak a hidden margin, so they're left as-is for now — a "show net for consumables too" consistency pass is optional.

  **F2 is now complete (A–D + F5).** The full money-movement model — markup, per-supplier admin-controlled commission, consumables payouts, and end-to-end reconciliation — is implemented and reconciles. Open follow-ups: per-booking-type payout line items on the bill (Σ by day/week/month), a "Day Booking ×1"-style Stripe charge description, and the real Stripe→Xero API sync — all future, and the payout data model already supports them.
- [x] 🟠 **F3 (Medium, correctness) — bulk-order spend uses the pre-split combined balance and is invisible to both split balances.** ~~`fulfillBulkOrderWithDebit` checks `assertSufficientBalance` (→ `getCreditBalance`, which sums **all** transaction types with no filter) and writes type `purchase`, which is in **neither** `getPurchasedBalance` nor `getEarnedBalance`. Two consequences: (a) `getCreditBalance` is polluted by `booking_payment`/`refund` rows — a user's card-funded booking charges reduce the wallet balance a bulk-order fulfillment checks against, even though bookings are Stripe-funded and shouldn't touch the wallet; (b) a third-party operator's bulk order can be funded from earned credits, the exact MAS Payment Services Act exposure the purchased/earned split was built to prevent. Bulk orders were never rewired to the split model (Buy Now was).~~ **Resolved 2026-07-25 — by taking bulk orders off the credit ledger entirely, not by rewiring the debit.** Product owner clarified the real model: bulk orders are a Just-In-Time **lead** — a member's request routes to the supplier (notification already built), who negotiates the RSP and settles **off-platform**. SpaceSnap takes **no cut** and **no credits move**. So the whole on-platform financial apparatus (confirm-time `CreditHold` + available-balance gate, fulfillment wallet debit `purchase`, and F2 Part C's 7% consumables `SupplierPayable`) is now gated behind `BULK_ORDER_ON_PLATFORM_SETTLEMENT` (`lib/bulk-orders.ts`), **default off**. Off-platform, `confirmBulkOrder`/`fulfillBulkOrderWithDebit` are pure status transitions + audit-log entries — no `getCreditBalance` call, no `purchase` Transaction, no payable — so both F3 consequences are eliminated by never touching the wallet. The path is **shelved, not deleted** (per the "don't delete unbackended paths" convention): flip the flag (const or `BULK_ORDER_ON_PLATFORM_SETTLEMENT=true` env) to reactivate, and it stays under test — `lib/credit-holds.test.ts` forces it on file-wide, and a dedicated "shelved on-platform settlement" describe in `lib/bulk-orders.test.ts` flips it on around its tests. New off-platform assertions prove no debit/hold/payable is created; the shelved-path assertions (incl. the 7% payable) still pass. A prominent ⚠️ on the flag notes F3 must be re-fixed (split-balance debit) *before* any reactivation. Small buyer-facing copy fix too: `RequestPurchaseModal` bulk success/estimate no longer claims "credits will be charged" — it says the supplier will follow up directly and labels the figure "Estimated value / indicative only." Buy Now (small quantities, on-platform, 7% commission) is unchanged. `tsc` clean; 56 tests green across bulk-orders/credit-holds/purchases/supplier-payables.
- [x] 🟡 **F4 (Medium, compliance flag for go-live) — `purchasedBalance` is not backed by collected SGD.** ~~Wallet top-ups write `purchased_topup` with no real Stripe charge (credits-only in this build), yet `purchasedBalance` funds Buy Now and bulk orders. Expected in dev, but must be closed before real money: "purchased" credit currently represents no actual money received.~~ **Resolved 2026-07-25 — Stripe now backs top-ups.** The Stripe integration existed only for booking charges; the top-up path (`createTopUp`, `lib/wallet.ts`) still minted `purchased_topup` credit with no charge. Now it reuses the exact booking pattern: `POST /api/wallet/topup` carries a `paymentMethodId` (pm_… from Stripe Elements), `createTopUp` creates a confirmed PaymentIntent (`toStripeCents`, SGD) **before** writing the ledger row, verifies `status === "succeeded"` (else `StripeChargeFailedError` → HTTP 402), stores the `stripePaymentIntentId` on the `purchased_topup` row, and on a post-charge DB failure fires a compensating refund (same discipline as `createBookingWithDebit`). `TopUpCreditsModal` now mounts the shared `StripeCardField` kit (card entry → pm id), shows per-preset SGD + a live "your card will be charged S$X" line, and requires a card; the stale "credits-only" copy on the modal and the wallet "Payment Methods" card is gone. Webhook: `purchased_topup` added to `PAYMENT_INTENT_TRANSACTION_TYPES` (`lib/stripe-webhooks.ts`) so a succeeded/refunded top-up reconciles against its own row instead of tripping the "charged with no matching row" alarm. The two shared Stripe error classes moved from `lib/bookings.ts` → `lib/stripe.ts` (re-exported from bookings so existing importers are untouched). Proven against the real Stripe **sandbox**: 9 passing tests in `lib/wallet.test.ts` (success charges + records a PaymentIntent id, `pm_card_chargeDeclined` throws and writes nothing, repeat/decimal amounts, required-paymentMethodId validation); booking decline-path test re-verified through the re-exported class; `tsc` clean. In-browser: the top-up modal renders the live Stripe card field (card number entered and validated as Visa), correct copy, SGD amounts, and the dynamic charge preview — a full click-through charge wasn't completed only because automated text entry into the cross-origin Stripe expiry iframe was unreliable (harness limitation, not a code issue; the end-to-end charge is covered by the sandbox DB tests). **Note (unchanged go-live posture):** this is real payment code — the doc's "no live payment code merged without a second reviewer" rule still applies before production.
- [x] 🟡 **F5 (Low, product decision, blocks F1 sign-off) — define "revenue by operator." DONE 2026-07-30.** ~~Even after F1 is fixed, `booking_payment` is the *gross* member-paid amount (includes SpaceSnap's commission). Decide whether the operator-facing revenue figure should be gross-through-their-listings or net-of-commission — resolve alongside F2 (persisting commission makes the net figure derivable).~~ Product owner specified three distinct visibility tiers, confirmed and built: **(1) individual (non-admin) supplier staff** — net earnings only, never markup/commission — already true (`GET /api/supplier/revenue/by-type`, scoped to `companyId`, `requireSupplier()`-gated, only ever returns `SupplierPayable.netAmount`), no change needed. **(2) Company admin** — same net-only company total by default, plus a new admin-only "By Supplier" toggle on the "My Earnings" card (`app/(supplier)/supplier/page.tsx`) breaking net earnings down per individual staff member, attributed via a new `Listing.ownerId` (nullable `String?` FK to `User`, migration `20260730090517_listing_owner_id`, no backfill — pre-existing listings show as "Unassigned"; defaults to the creating user on `POST`, reassignable only by a company admin via a new "Owner" dropdown in `AddEditListingModal.tsx`, gated by `requireCompanyAdmin()` + same-company check on the PATCH route). New `getCompanyNetPayoutByOwner` (`lib/revenue.ts`) + `GET /api/supplier/revenue/by-member`. **(3) System admin** — the admin Financials "Revenue by Operator" table now shows 4 columns (Gross/Markup/Commission/Supplier's Cut) instead of one, **recognized at booking/sale completion** (the existing audited `SupplierPayable` ledger — same reconciliation already used for the Supplier Payouts card on the same page) rather than at charge time, per explicit product-owner confirmation of that timing tradeoff. New `getCompanyRevenueBreakdown` (`lib/supplier-payables.ts`) replaces the old Transaction-based `getRevenueByCompany` (deleted, along with its dedicated test block — the underlying `REVENUE_TRANSACTION_TYPES` regression coverage was preserved by re-targeting those assertions at `getRevenueTransactionFeed`, since `getPlatformRevenueSummary` still depends on that same shared code path). 22 new/extended tests across `lib/supplier-payables.test.ts`, `lib/revenue.test.ts`, and new `lib/listings.test.ts` — full suite (567 tests) green, `tsc` clean. Verified live end-to-end: non-admin (Chandra) sees no toggle; company admin (Ben) sees the toggle, reassigns a listing's owner via the modal, and the "By Supplier" chart immediately reflects it (live join, not a snapshot) — its total matches the pre-existing "By Listing Type" chart's total exactly (-30 = -30); system admin (Alice) sees the 5-column table with `gross == markup + commission + supplierNet` holding for every row, consistent with the existing Supplier Payouts reconciliation strip on the same page. Test edit reverted after verification, dev DB left in its original seeded state.

### Confirmed sound (do not re-flag)

- Booking charge/refund atomicity: Stripe call outside the Prisma txn with a compensating refund on DB failure; the one un-compensated leftover-credit-refund race is explicitly flagged in-code, not silent.
- `stripe-webhooks.ts` is a genuine reconciliation safety net (detects charge-succeeded-but-no-ledger-row and Stripe-vs-ledger refund-total mismatch), not a primary write path.
- `SupplierPayable` → `SupplierPayout` batch math snapshots correctly and nets supplier penalties against earnings via a live SUM.
- Reward-tier rebates / referral bonuses / earned-credit reversals are ledger-only `earned_grant`/`earned_spend` entries (promotional issuance, no real-money leg) — correct not to appear as revenue.

---

## Closed loop

This doc's Part 1 is the completion of the old sprint plan's **"Sprint 7.1: Site-Wide UI→Backend Wiring Audit"** (raised 2026-07-23, never started) — that item and its file section in `SPRINT_PLAN_NEXTJS_REWRITE.md` can now be considered done, superseded by the findings above.
